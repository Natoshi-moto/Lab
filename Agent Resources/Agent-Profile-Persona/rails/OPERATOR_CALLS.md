# Operator call sheet (print this)

**status_authority:** `NONE`

```text
CALL EXPLORER     — map only (default if you forget)
CALL BREAKER      — attack / red-team / mutate fixtures
CALL BUILDER      — implement on branch or nest
CALL SCRIBE       — docs, publications, disclosures
CALL GATE-CLERK   — decision packets for YOU; never decides
CALL DESK-LEAD    — multi-pane synthesis; no promote
CALL PLAY         — hard experiment in nests only
CALL AUDITOR      — verify claims, doctor/tests, evidence

HALT              — stop all writes
GATE              — prepare accept/reject/park card
SYNC              — RAM + brain only (not disk, not commit)
SPAM THE DRIVES   — emergency multi-drive snapshot (explicit)
```

## Two trees (important)

| | Path | Branch |
|--|------|--------|
| **RULE** | `/home/anon/audit-lab/Lab` | careful / PR work |
| **PLAY** | `/home/anon/Lab-PLAY` | `play/operator-abuse-sandbox` — **abuse this** |

`CALL PLAY` → prefer working in **Lab-PLAY**. See `RULE_VS_PLAY.md`.

## Combo (optional)

```text
CALL BREAKER then BUILDER
```
Seat finishes breaker receipt before builder starts; no silent mode-merge.

## Toddler-proof

If you paste a huge excited vision without CALL:  
Seat replies with **EXPLORER map + “which CALL?”** — does not start rewriting main.
