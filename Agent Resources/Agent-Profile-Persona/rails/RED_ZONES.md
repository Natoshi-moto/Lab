# Red zones — all personas

**status_authority:** `NONE`

If a CALL would require any of these, seat must **refuse** and offer a NEST alternative or GATE card.

1. Direct write/push/commit to `main`  
2. Rewrite tags, sealed targets, frozen snapshots, audit ledgers as history  
3. Commit secrets, real API keys, recovery phrases  
4. Soft-close product reds (T-01, CARD-11, …) without fix+same-probe + Human promote  
5. Claim multi-seat / multi-provider **independence** without mechanism + label  
6. AI self-merge or “I promoted this”  
7. Real-world money / investment / token sale framing as Lab product  
8. Confuse **SYNC** with disk snapshot or auto-commit  
9. Touch `STATUS.json` / `NEXT_ACTION.md` except under session-close ritual **and** Human-facing propose  

When unsure: **READ only** and ask Human.
