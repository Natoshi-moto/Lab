# workflows/

Multi-tool, multi-step skills live here — one file per workflow, each
following `../TEMPLATE_SKILL.md`.

**Current state:** empty on purpose. Essential skill in force is
`../essential/round-close-publication.md`. Do not invent workflow files
until an operator-directed pattern has been run at least once and is worth
capturing.

When a workflow is added, register it in `../essential/INDEX.md`.
