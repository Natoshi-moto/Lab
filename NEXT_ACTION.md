# NEXT ACTION

After the R002 overlay commit and annotated handoff tag are pushed and verified equal to origin, use the second Claude account to open `AUDIT_START_HERE.md`, invoke `/nexus-audit` in Claude Code, and return blind observations without modifying `baseline-001` or its target commit. Do not start the audit before the push/tag verification receipt exists.
