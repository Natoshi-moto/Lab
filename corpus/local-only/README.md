# LOCAL_ONLY payloads

Plaintext files placed in this directory are ignored by Git. Commit only a manifest under `corpus/records/artifacts/` containing a content hash, size, storage location class, and retrieval policy. Never commit the local absolute path or secret material.
