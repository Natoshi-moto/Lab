# Non-claims and open questions — BGEN-ECON-REDTEAM-001

## Non-claims

This package (`experiments/BENEFICIAL_GENESIS_ECON_REDTEAM_001/**`, `operations/audits/BENEFICIAL_GENESIS_ECON_REDTEAM_001/**`, `operations/receipts/BENEFICIAL_GENESIS_ECON_REDTEAM_001/**`) does **not** establish:

- any real-world market price, adoption level, donation volume, or charity behavior — every "value," "multiplier," "discount," or "utility"/"opportunity value"/"realization fraction" figure is a declared modelling assumption applied to synthetic populations, never an empirical estimate or forecast;
- that modelled attacker strategies are empirically observed — they are worst-case rational-actor constructions used to test whether a mitigation exists in principle, not evidence that any real actor has attempted or will attempt them;
- a change to, or independent verification of, the Beneficial Genesis cryptographic verifier (`experiments/BENEFICIAL_GENESIS_DESIGN_001/protocol/**`) — this package's simulator is entirely separate code and touches no verifier path;
- legal conclusions of any kind — "legal/economic issue surfaces" identified here (e.g. the fixed-pool floating implied allocation ratio behaviour in `MECHANISM_NECESSITY.md` §2, or the rebate/circularity findings) are flagged for qualified counsel review per Track F of issue #33, not adjudicated. This package uses neutral terms such as "fixed-pool floating implied allocation ratio" and never characterizes the mechanism as a "token sale" or "investment contract" (E-006 repair);
- authorization for live funds, real charity selection, public solicitation, token issuance, or any R-round assignment;
- status promotion, merge authority, or any change to `STATUS.json`;
- that any modelled economic outcome (rebate incidence, stolen-fund migration profit, governance capture) is unconditional. Every such finding in `FAILURE_CONDITIONS.md` is labelled with an explicit evidence class (mathematically proven / conditional simulation result / structural risk / empirical unknown / policy-legal question); none is presented as a bare fact;
- completeness of the adversarial scenario list — the required scenarios in issue #34 were each addressed by at least one manifest, but the space of possible strategic behavior is not exhaustively enumerable, and combinations of probes (e.g. a whale who is *also* Sybil-splitting *and* rebate-colluding *and* racing a compromise cutoff) were not jointly simulated;
- bit-identical reproducibility across all Python builds for the `CONCAVE_LOG` scheme specifically, since it uses `math.log1p` (IEEE-754 float); every other scheme and every metric use exact integer or `fractions.Fraction` arithmetic and are reproducible bit-for-bit on any conforming Python 3 implementation (verified by `tests/test_scenarios.py::TestScenarioDeterminism`, which re-runs every manifest twice and asserts identical output).

## Explicit modelling assumptions (restated from `FORMAL_MODEL.md`)

1. Token value is expressed only as a multiplier on a reference *breakeven* price `p0 = total_eligible_units / pool_units`; no absolute price is asserted.
2. The lock-up illiquidity discount (7%/month, capped at 70%) is an assumed parameter chosen to illustrate direction and rough sensitivity, not a calibrated or sourced number; it is reused consistently by `model/tainted_funds.py` rather than introducing a second, inconsistent discount schedule.
3. Synthetic donor populations are generated from a seeded `random.Random`, not sampled from any real donation data; population sizes and sat ranges were chosen to make concentration and attack effects legible, not to match any anticipated real distribution.
4. "Group" (beneficial owner) assignments for Sybil-split and exchange-omnibus scenarios are asserted by the manifest author (this seat), not inferred from any real on-chain heuristic — this package does not implement or claim any real entity-clustering technique.
5. Charity "honesty," "collusion," or "control" are boolean/parametric labels on synthetic donors for modelling purposes; no real charity is named, modelled, or implied.
6. Stolen/tainted-fund economics (`model/tainted_funds.py`) assume default values for alternative-realization fraction (0.7), seizure probabilities on the alternative (0.3) and migration (0.2) paths, and a liquidation haircut (0.15). All are declared assumptions swept over a sensitivity grid (`10_stolen_key_donation`'s `tainted_fund_sensitivity_grid`), never calibrated facts about any real attacker's cost structure.
7. Rebate access frictions (`model/collusion.py`) default to "arrangement already exists" (access probability 1, zero cost, full enforcement, zero detection) unless a scenario explicitly opts into lower access (e.g. `27_rebate_access_frictions`). This default preserves the original conditional-arithmetic finding while making the friction-free assumption visible rather than implicit.
8. Governance weight is computed **only** for scenarios that explicitly request it via `governance_rules`; no scenario defaults to assuming any particular governance-integration rule (E-003 repair).

## Open questions for later gates (Tracks B-F of issue #33)

1. **Does a governance cap, if adopted, remain enforced once tokens are transferable and any lock-up expires?** This requires a Track E (ledger integration/consensus boundary) answer this seat cannot provide; FC3 in `FAILURE_CONDITIONS.md` treats this as unresolved rather than assuming either answer.
2. **Is sealed/precommitted donation (removing the denominator-timing exposure in FC5) compatible with the real Bitcoin transaction model (Track B)?** Not evaluated here; this package works only in the abstract sats/units domain already used by `BENEFICIAL_GENESIS_DESIGN_001`.
3. **What is the real magnitude of the charity-rebate/circularity risk?** This package shows the *conditional* arithmetic is exactly one-for-one and the *expected* (friction-adjusted) magnitude falls sharply once access is rare and costly (`27_rebate_access_frictions`); it does not and cannot estimate what fraction of a real donor base would actually find or create a colluding charity destination against an immutable genesis set — that is a social/legal question for Track D/F, not a simulable economic one.
4. **What is the real magnitude of the stolen/tainted-fund migration risk?** The pathway exists (crypto control ≠ legal ownership) and net profitability is assumption-conditional (`model/tainted_funds.py`); this seat cannot estimate real seizure probabilities, real alternative-disposition markets, or real attacker risk tolerance.
5. **Would a hybrid design (transferable economic unit, non-transferable/capped governance, non-transferable window, sealed commitment) fully resolve all seven failure conditions, or only some of them?** `ALTERNATIVES_COMPARISON.md` §5 proposes this combination as the least-bad *if* a token is retained at all, but it has not been simulated as a single integrated scheme — only its components have been tested individually.
6. **Which target-ledger functions (payment, staking, fees, capital allocation, bootstrap) does the program intend the allocation unit to serve?** `MECHANISM_NECESSITY.md` §3a states this is unspecified in the design pack and is a prerequisite for closing FC1, not something this seat can assume either way.
7. **How does this analysis interact with Track B's real-Bitcoin and Track C's real-PQ work?** Not at all by design — this seat was explicitly scoped to change no cryptographic verifier field except where an economic assumption exposes a required protocol field or explicit non-claim, and no such exposure was found.

## Second-family review

**Update (BGEN-ECON-REPAIR-002, issue #38):** a second-family (Grok) Breaker/Reproducer review of this package's original submission (PR #35 @ `b5887791338b146daad8f5233ce0e25bf24fe357`) has now occurred, independently and via clean-room reconstruction (`BENEFICIAL_GENESIS_ECON_BREAKER_001`, merged as PR #37 at `d8523b29ca7a1e0433ab5afdb494ed8452450dde`). Its findings — recorded in the controlling review on PR #35 and in that package's own `results/DIFFERENTIAL_REVIEW.md` and `results/DECISION.json` — confirmed six overclaim/implementation items (BGEN-ECON-REV-001 through 006) against the original submission and preserved seven narrower findings (BGEN-ECON-REV-007). This repair (E-001 through E-009) implements the required corrections. See `CROSS_MODEL_COMPARISON.md` for a side-by-side comparison of the repaired Claude model against the frozen Grok model on the preserved findings.

This repaired package has **not** itself been re-reviewed by a second family; the review above applies to the pre-repair submission. The repair is disclosed as a single-seat (Designer/Red-Team) correction responding to that review, not as a new independent confirmation of the repaired result. Any operator decision to accept this package as an `ECONOMIC_GATE_PASS` should still require a fresh second-family look at the repaired package, per issue #34's own requirement; this repair's recommendation is `CONTINUE_WITH_CONDITIONS` with `ECONOMIC_GATE_PASS: false`, not a gate pass, so that requirement is not itself triggered by this submission.
