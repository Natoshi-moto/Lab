# Decisive next step — BGEN-INTEGRATION-TRIBUNAL-001

## Controlling first action

**Execute repair R1 (scheme-parameter validation + supply-invariant gate
in the REDTEAM economics package) as a bounded task, then obtain a fresh
independent retest of the exact repaired commit.**

Why this controls, given that the *research-decisive* step is S1
(product-function specification):

1. R1 is the only confirmed blocker of evidence-package integration
   (TRIB-F-001). Until it lands, the record's flagship claim — "the
   repaired economics package upholds its supply invariant" — is
   falsified by a one-line input, and every downstream citation of the
   package inherits the defect.
2. R1 is hours of work with a fully specified acceptance test set
   (REPAIR_ACCEPTANCE_PLAN R1); S1 is a judgment-heavy specification task.
   Sequencing the mechanical repair first means the specification round
   starts from a clean evidence base instead of a known-falsified one.
3. R1's retest also discharges R2/R3 cheaply (the retest receipt should
   document `npm ci` and the corrected README counts), collapsing three
   ledger items into one bounded round.

## The sequence after the first action

1. **R1 + R2 + R3 (+R4 optional)** — bounded repair commit(s).
2. **Fresh independent retest** of the repaired package (different
   session; receipt binds the exact commit; PR #39 pattern).
3. **S1 — product/ledger function specification with executable
   acceptance table** (the step all three mechanism-facing audits
   independently named). Its output mechanically decides S2 (transfer
   default), and must record S3 (issuance policy), S4 (identity stance),
   S5 (governance separation).
4. **S6 — preregistered empirical participation/flow-composition design**,
   only after S1 fixes what product the elasticity would be measured for.

## Stop/redirect conditions (inherited from the record, kept in force)

- If the S1 regime matrix shows no design serves the required function
  set within proven constraints → STOP (record's own condition).
- If S6-grade evidence shows near-zero or adverse induced giving → STOP.
- If a required function is shown (executably) to need transferability →
  reverse the S2 default with the falsifier attached.

## What must NOT be done next

- No implementation of repairs inside audit/tribunal packages.
- No citation of the record as "five independent AIs agreed"
  (TRIB-F-018 language rule).
- No real-world activity of any kind: RESEARCH_ONLY stands.
