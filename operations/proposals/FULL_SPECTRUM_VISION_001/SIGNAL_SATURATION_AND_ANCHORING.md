# Signal saturation and salient anchoring

**Status:** `PROPOSAL / STATUS_AUTHORITY: NONE`  
**Cites:** CLAIM-SEM-001…003, CLAIM-METH-001, OP-03, OP-07, OP-08  
**Genesis:** `baseline-001`

---

## Philosophy

**OP-07:** New coding philosophy of **SIGNAL SATURATION** and **SALIENT ANCHORING**, tied to LOOM grepping + FFD, toward a semantic compilable agent/execution/contract layer.

| Pillar | Rule |
|--------|------|
| **Saturation** | Prefer dense **relevant** engineer context over thin tribal code (**OP-03** FFD bloat) |
| **Anchoring** | Sparse high-contrast greppable markers; if everything is bold, nothing is |

---

## Coordinate anchors

**OP-08:** spatial, temporal (correct term), project ID; every tiny security function mapped; open source think TailsOS.

| Axis | Answers |
|------|---------|
| Spatial | product, ring/zone, boundary, trust surface |
| Temporal | since, last verified, session/record id, era |
| Project | FULL_SPECTRUM, NOTED_HOST, NEXUS_OS, LOOM, HERMES_TEACHER, … |
| SEC-ID | tiny security job card |

### SEC-ID card fields (v0)

```text
@sec-id @spatial @temporal @project
@threat @mechanism @verify @residual @nonclaim
```

UNVERIFIED is allowed; silent green is not.

---

## Grep as ABI

**OP-10 / MGR-03:** Grep anchors, translates, bridges semantic world ↔ legacy code.

If a decision cannot be `rg`'d, it is not in the circuit.

---

## FFD relationship

FFD is **how** saturation is written: always walk the code; enrich blocks; exemplify human–AI collaboration (**OP-03**).

---

## Future contract layer (direction only)

Intent + claims + caps + generative step + verify + receipt — under hostile-machine axiom. Not shipped IR in this pack.
