# NEXT ACTION

Implement and attempt to falsify R012 bounded work exchange V0 on the proposal branch.

The commissioned candidate is one local, non-financial, non-fungible and non-transferable accepted work return, bound to an exact task, generated route, baseline, routed synthetic source manifest and predecessor state. Its useful output is a source-bound cognition shadow that a cold-context consumer can turn into the frozen HBR-17 decision brief.

Proceed in the order declared by `operations/proposals/R012_BOUNDED_WORK_EXCHANGE/BUILD_PACKET.md`. First complete the schema/source bootstrap and instantiate the strictly schema-compatible task without changing its pinned main baseline. Generate the route; do not hand-author it. Then implement the bounded pack, validator, ledger and adversarial suite.

Stop on a kill criterion. Preserve dissent and failure evidence. Do not weaken pre-registered gates, assign financial/PCCE/identity/consensus semantics, modify canonical history, merge, or promote. If all gates pass, prepare a review-ready receipt with `status_authority: NONE` and ask the user for disposition.
