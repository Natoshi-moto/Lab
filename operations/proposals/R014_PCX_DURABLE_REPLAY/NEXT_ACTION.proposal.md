# Proposed next action for R014

Review the implemented durable-replay store and the complete crash, retry,
concurrency, corruption and anchor campaign on the exact proposal head.

Do not advance to real keys, custody, network consensus or economic backing to
make the demonstration more impressive. This round closes only the local
process-recovery gap in the promoted R013 synthetic conservation experiment.

The local gates pass and the evidence report is frozen for the bounded claim
`DEMONSTRATED_CRASH_CONSISTENT_LOCAL_RECOVERY_OF_SYNTHETIC_CONSERVED_STATE`.
Promotion remains a separate user decision. The R001 canonical snapshot
remains unchanged.

If any kill criterion fires, preserve the failing database and fault trace,
leave R014 unpromoted and open a defect. Do not repair, truncate, reinitialize,
move expected results or weaken the declared fault model silently.

Even after a pass, the next responsible gate is an independent cold verifier
for durable records and anchors plus a bounded crash-lifecycle model. Only
after that should a separate round introduce synthetic offline signing,
transaction display, backup and recovery. Networking, global ordering,
backing/redemption and external audit remain separate workstreams and must not
inherit claims from R014.
