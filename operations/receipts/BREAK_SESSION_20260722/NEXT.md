# What’s next after BREAK session 1

## Where we are now

The workshop is clean enough to poke on purpose:

- Machine checks work  
- Dashboard says break-prep  
- Old Agent not launched from current ship  
- Runbook exists  
- Session 1 safe cards done and recorded  

## Next phase (in order)

### Phase A — Optional: put this receipt on `main`

Merge the PR that adds this folder so every AI seat sees session 1 results.

### Phase B — BREAK session 2 (harder, still synthetic)

Run later cards from the runbook, carefully, one at a time:

1. CARD-04 — can the Agent iframe reach host storage? (highest concern)  
2. CARD-06 / 07 / 08 / 09 — deeper bridge behavior (only if needed)  
3. CARD-11 — where a **fake** key would land (never a real key)  

Same bus: Grok gives one step → operator runs → paste result.

### Phase C — Decide after evidence

Human chooses, using stop-the-line / threat list:

- Measure-only for a while, or  
- Authorize small fixes (e.g. pin/vendor CDN, kill default proxy, tighten postMessage), each with re-probe  

No silent “we’re fixed” without re-running the same checks.

### Parked (not next unless you say so)

- NWC formal candidate (research shelf)  
- Full CRL-BRAID / GITBRAID continuity build  
- Replacing Agents from other folders  
- Money / token / real-value work  

## Operator one-liner

**We finished the easy honesty checks. Next is the harder “can the chat window walk around the door?” check, when you want it.**
