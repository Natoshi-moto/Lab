# DELIVERY REPORT — Sweep 56 — AI Brief Export + FocusContext Foundation + Branding

BOOT-SEQUENCE-ACTIVE v1

## 1. Technical summary

Implemented Sweep 56 on top of the accepted Sweep 55-G GitHub-candidate bundle.

- Added `src/focus/FocusContext.tsx` with `FocusRecord`, `FocusProvider`, `useFocus`, and `useSetFocus`.
- Wrapped the app shell in `FocusProvider`.
- Updated Nexus Panel to read FocusContext first, with Workspace `activeRecord` fallback.
- Updated active record surfaces to set FocusContext and existing `ws.setActiveRecord` together where surgical.
- Added `src/topology/aiBriefExport.ts` for export-only AI brief generation.
- Added Prompt Studio **Export AI brief** button with `data-test="ai-brief-export"` and a local-export privacy explainer.
- Set locked user-facing tagline: `Private by default. Relational by design. Portable by right.`
- Updated public docs/governance with Sweep 56 canon, risks, and deferred items.
- Rebuilt normal generated artifacts via `npm run build`.

## 2. Archive/waiver status

- External archive required: no.
- Base bundle used: Sweep 55-G GitHub-candidate bundle.
- Publication posture: unchanged. Sweep 55-G remains candidate-only; no publication/release/license gate was altered.

## 3. Files concerned

| File/path | Change |
|---|---|
| `src/focus/FocusContext.tsx` | New ephemeral focus provider/hooks. |
| `src/topology/aiBriefExport.ts` | New export-only AI topology brief helper. |
| `src/App.tsx` | Wrap shell with `FocusProvider`. |
| `src/appMeta.ts` | Set locked Noted tagline. |
| `src/components/NexusPanel.tsx` | Use FocusContext as primary focus source, fallback to `activeRecord`. |
| `src/components/ListItemMenu.tsx` | Set FocusContext alongside `activeRecord` for link-pick compatibility. |
| `src/components/LinksPanel.tsx` | Set FocusContext alongside `activeRecord` before link-pick. |
| `src/studios/writing/WritingStudio.tsx` | Set FocusContext for selected document. |
| `src/studios/notes/NotesStudio.tsx` | Set FocusContext for selected note. |
| `src/studios/poetry/PoetryStudio.tsx` | Set FocusContext for selected poem. |
| `src/studios/longform/LongformStudio.tsx` | Set FocusContext for selected longform doc. |
| `src/studios/projects/Projects.tsx` | Set FocusContext for selected project. |
| `src/studios/appdesign/AppDesignStudio.tsx` | Set FocusContext for selected build. |
| `src/studios/prompts/PromptStudio.tsx` | Set FocusContext for pipeline and add AI Brief Export UI. |
| `src/studios/library/Library.tsx` | Set FocusContext for selected pattern. |
| `README.md`, `NOOB_GUIDE.MD` | Added tagline and AI Brief Export notes. |
| `PREFLIGHT.MD`, `AI_README.MD`, `AI_CODEBASE_HANDOFF.MD`, `CONTROL_ROOM_STATE.MD`, `LANDMINES.MD`, `HANDY_LESSONS.MD`, `PUBLIC_REPO_MANIFEST.MD`, `PUBLICATION_CHECKLIST.MD` | Governance/publication notes updated for Sweep 56. |
| `dist/`, `noted-v0.01.html`, `verse-studio.html` | Regenerated by build. |
| `verification/sweep56/` | Verification logs and sample JSON parse artifact. |

## 4. Protected invariants

| Invariant | Status |
|---|---|
| `DB_VERSION = 10` | Preserved. |
| `SCHEMA_VERSION = 9` | Preserved; AI brief imports this value. |
| IndexedDB store set/migrations | Not changed. |
| `noted-v0.01` app identity | Preserved. |
| `verse-studio:*` storage prefix | Preserved. |
| Package manifests/dependencies/lockfile | Not changed. |
| Import/writeback behavior | Not changed. AI Brief Export is export-only. |
| Diagnostic/export verification infrastructure | Not changed. |
| Build identity/hash logic | Not changed. |
| GitHub candidate gates/publication status | Preserved; no release/publication/license choice made. |
| Nexus Panel feature name | Preserved. |

## 5. Evidence requested by Orchestrator

- FocusContext provider exists and is wired into the app shell.
- Nexus Panel reads FocusContext as primary focus source and falls back to existing `activeRecord`.
- Existing command/link active record compatibility remains in source.
- AI Brief Export helper creates the required v1.0.0 envelope with canonical system prompt, allowed node/edge vocabularies, `content_depth: "titles_summaries"`, `workspace_schema_version: SCHEMA_VERSION`, tags/projectIds/aiLineage, and local content hashes.
- Prompt Studio exposes `data-test="ai-brief-export"`.
- Export helper does not import, mutate, write back, call network, or call AI APIs.
- Branding tagline is set without app/storage identity changes.
- Source/build verification passed.

## 6. Commands run and results

```bash
npm ci
```

Result: PASS. Installed from existing lockfile. Existing audit note remained: 2 moderate vulnerabilities. No fix run.

```bash
npm run typecheck
```

Result: PASS. `tsc --noEmit` completed cleanly.

```bash
npm run build
```

Result: PASS. Vite built 284 modules and `pack.js` emitted `verse-studio.html` + `noted-v0.01.html`.

```bash
rg -n "FocusContext|FocusProvider|useFocus|focusedAt|origin" src
rg -n "activeRecord|setActiveRecord" src/context.tsx src/components src/studios
rg -n "nexus_topology_version|allowed_node_types|allowed_edge_labels|content_depth|system_prompt|ai-brief-export" src
rg -n "Private by default\. Relational by design\. Portable by right\." src README.md NOOB_GUIDE.MD PREFLIGHT.MD AI_README.MD AI_CODEBASE_HANDOFF.MD
rg -n "DB_VERSION = 10|SCHEMA_VERSION = 9" src/db.ts
rg -n "noted-v0.01|verse-studio" src README.md PREFLIGHT.MD AI_README.MD AI_CODEBASE_HANDOFF.MD
```

Result: PASS / reviewed. See `verification/sweep56/logs/required_greps.log`.

```bash
sha256sum -c verification/sweep56/logs/package_before.sha256
```

Result: PASS. `package.json` and `package-lock.json` unchanged.

```bash
rg -n "fetch\(|XMLHttpRequest|WebSocket\(|EventSource\(|OPENAI|ANTHROPIC|GEMINI|api[_-]?key|sk-[A-Za-z0-9]|ollama|/api/" src || true
```

Result: REVIEWED. No AI/network implementation found. One false positive came from the text `task-switch` matching the broad `sk-` token pattern.

```bash
node verification JSON.parse sample
```

Result: PASS. `verification/sweep56/sample-ai-brief.json` parsed successfully. The Prompt Studio export handler also stringifies and parses the generated AI brief before download.

## 7. Package/dependency status

- `package.json`: unchanged.
- `package-lock.json`: unchanged.
- No dependency additions/removals/upgrades.
- No `npm audit fix` run.

## 8. Generated artifact hashes

```text
3db8191093ee89892b337ac57ab44a67ecdd55b7f78b99778a4910be325390e9  noted-v0.01.html
3db8191093ee89892b337ac57ab44a67ecdd55b7f78b99778a4910be325390e9  verse-studio.html
1a67369b45cd1b8a0c5189334c3e53ad8f87a71db8b2a26dcc864c3ff6d6f3ed  noted-v0.02.html
60578fa957d321dfa964e82bd3e2e4e73a948ffb3a69db591a214dfd8793b0eb  dist/assets/index-CEg2b6Sb.js
296d4c785dad5d5ca21053c54779d43d03dd1de0a235fd673886ea30df9a80f9  dist/assets/style-i_N973ii.css
```

## 9. Runtime status

Runtime pending / manual verification required.

No browser/Electron smoke was run. No runtime claim is promoted.

## 10. Risks/deferred items

- AI Brief downloads may contain private user titles/snippets if shared externally.
- AI import/writeback/round-trip/Ollama/API bridge remains deferred.
- DB v11/contentHash persisted schema/semantic color store/Noted Packet work remains deferred.
- Sweep 55-R runtime checks remain pending unless separately attested.
- GitHub publication remains gated by owner decisions and is not authorized by this sweep.
- Existing moderate dependency audit findings remain unresolved because dependency changes were not authorized.

## 11. Runtime/manual instructions

1. Open Noted and confirm Sidebar/app branding shows Noted and the locked tagline where intended.
2. Select/open a record in Notes and Prompt Studio; open Nexus Panel; verify it reflects the focused record.
3. Trigger **Export AI brief** from Prompt Studio; confirm a `.json` downloads.
4. Open the downloaded JSON and confirm:
   - valid JSON;
   - `nexus_topology_version` is `1.0.0`;
   - `content_depth` is `titles_summaries`;
   - counts are plausible;
   - `root` reflects current focus when a record is focused, otherwise `{}`;
   - no network/API call occurred;
   - existing record IDs appear as read-only references only.
5. Confirm existing Cmd/Ctrl+K and Cmd/Ctrl+L link-selection flows still work superficially.
6. Sweep 55-R runtime checks remain pending unless separately performed.

## 12. Artifact bundle contents and path/name

Bundle target name: `noted-v0.01-sweep56-ai-brief-focus-branding-builder-delivery.zip`.

Bundle includes current source/artifacts in scope, regenerated outputs, updated docs/governance, verification logs, sample JSON parse artifact, and this delivery report.
