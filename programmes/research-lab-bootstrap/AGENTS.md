# Programme-specific AI instructions

Read the root `AGENTS.md` first. This programme treats R001 as an audit-ready candidate, not a finished product. Preserve target bytes, state evidence limits, and route any proposed implementation mutation through a branch or returned patch. The active external task is observation-only.
