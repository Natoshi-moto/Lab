# Snapshots

`canonical/` contains deterministic `CANONICAL_AS_IS` ZIP targets and sidecar hashes. `audit-overlays/` contains later observations or reports that do not rewrite those targets. `restore/` contains recovery instructions, not a second authority store.
